[pytest]
addopts = -ra
pythonpath = .
markers =
    unit: fast unit tests
    integration: tests using service dependencies (Redis/Mongo)
    e2e: end-to-end smoke tests for running app
testpaths = tests
