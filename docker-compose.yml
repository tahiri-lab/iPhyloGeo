services:
  mongo:
    image: mongo:6.0
    ports:
      - 27018:27017
    volumes:
      - mongo_data:/data/db
      # - ./db/backup.agz:/database_backup/backup.agz # eventually restore from backup

  redis:
    image: redis:7-alpine
    ports:
      - 6379:6379


volumes:
  mongo_data:
